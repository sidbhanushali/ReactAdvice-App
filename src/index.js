import React from 'react'
import ReactDOM from 'react-dom'

import App from './App'



//injects our the App component which is out whole app, into the root div 
ReactDOM.render(<App/>, document.querySelector('#root'))